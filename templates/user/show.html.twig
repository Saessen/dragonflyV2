{% extends 'base-front.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>
                    <span class="col-7 text-end">Information</span>
                    <br>
                    <span class="col-9 text-end">{{ user.username }}</span>
                </h1>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-12">
                <h2 class="text-center">Pour profiter pleinement des fonctionnalités du site, complétez votre profil! </h2>
                <div class="col-lg-6">
                    <div>{{ user.username }}</div>
                    <div>{{ user.password }}</div>
                    <div>{{ user.nom }}</div>
                    <div>{{ user.prenom }}</div>
                    <div>{{ user.sexe }}</div>
                    <div>{{ user.email }}</div>
                    <div>{{ user.poste }}</div>
                </div>
                <div class="col-lg-6">
                    <div>{{ user.numeroRue }}</div>
                    <div>{{ user.rue }}</div>
                    <div>{{ user.codePostal }}</div>
                    <div>{{ user.ville }}</div>
                    <div>{{ user.pays }}</div>
                    
                </div>
            </div>
        </div>
    </div>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ user.sexe }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ user.poste }}</td>
            </tr>
            <tr>
                <th>Numero_rue</th>
                <td>{{ user.numeroRue }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ user.rue }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ user.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ user.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ user.pays }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ user.latitude }}</td>
            </tr>
            <tr>
                
            </tr>
        </tbody>
    </table> #}

    <a href="{{ path('user_index') }}">Retour</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">Modifier mes informations</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
