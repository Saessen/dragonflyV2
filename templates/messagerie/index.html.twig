{% extends 'base-front.html.twig' %}

{% block title %}Messagerie index{% endblock %}

{% block body %}
    <h1>Messagerie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Event</th>
                <th>Expéditeur</th>
                <th>Message</th>
                <th>CreatedAt</th>
               
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for messagerie in messageries %}
            <tr>
                <td>{{ messagerie.event.nom | upper}} </td>
                <td>{{ app.user.username}}</td>
                <td>{{ messagerie.message }}</td>
                <td>{{ messagerie.createdAt ? messagerie.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                
                <td>
                    <a href="{{ path('messagerie_show', {'id': messagerie.id}) }}">show</a>
                    <a href="{{ path('messagerie_edit', {'id': messagerie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('messagerie_new') }}">Create new</a> #}
{% endblock %}
